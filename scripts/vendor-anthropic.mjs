// Ponto de entrada para empacotar o SDK oficial da Anthropic em vendor/anthropic-sdk.mjs
// (usado pelo service worker do Assistente de IA). Rode: npm run build:vendor
export { default } from '@anthropic-ai/sdk';
