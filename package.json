{
  "name": "zapflow-extension",
  "private": true,
  "type": "module",
  "version": "1.3.1",
  "description": "Ferramentas de desenvolvimento da extensão ZapFlow (a extensão em si não precisa de build).",
  "scripts": {
    "build:vendor": "esbuild scripts/vendor-anthropic.mjs --bundle --format=esm --platform=browser --target=chrome111 --minify --legal-comments=eof --outfile=vendor/anthropic-sdk.mjs",
    "test": "node tests/util.test.mjs && node tests/actions.test.mjs && node tests/background.test.mjs && node tests/backup.test.mjs",
    "harness": "node dev/server.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
